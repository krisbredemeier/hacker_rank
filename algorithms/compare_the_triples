#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main(){
    int i = 0;
    int total_a = 0;
    int total_b = 0;
    int *a, *b;
    int a0; 
    int a1; 
    int a2; 
    scanf("%d %d %d",&a0,&a1,&a2);
    int b0; 
    int b1; 
    int b2; 
    scanf("%d %d %d",&b0,&b1,&b2);

    //while (a[i] != '\0' && b[i] != '\0')
    //{
    //    if (a[i] > b[i])
    //    {
    //         total_a +=1; 
    //    }   
    //   else if (a[i] < b[i])
    //    {
    //        total_b +=1;
    //    }
    //    i++;
    //}

    (a0 == b0) ? : ((a0 > b0) ? total_a++ : total_b++);
    (a1 == b1) ? : ((a1 > b1) ? total_a++ : total_b++);
    (a2 == b2) ? : ((a2 > b2) ? total_a++ : total_b++);
    printf("%i %i", total_a, total_b);
    return 0;
}

